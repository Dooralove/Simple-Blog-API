# SimpleBlogAPI

SimpleBlogAPI - это RESTful API на Spring Boot для управления статьями блога. API позволяет получать статьи по тегу, а также предоставляет базовую информацию о статье, включая название, контент, дату, лайки, дизлайки и комментарии.

📌 Функциональность

   -  Управление статьями: создание, удаление, получение статьи по тегу.
   - Управление комментариями: добавление, удаление комментариев к статьям.
   - Управление тегами: создание, удаление, поиск и получение тегов по их ID или имени.

## 🛠️ Технологии

- **Java 17+**
- **Spring Boot**
- **Spring Web** (REST API)
- **Jackson** (для работы с JSON)
- **Spring Data JPA (для работы с PostgreSQL)**
- **PostgreSQL (для хранения данных)**
- **Aspect-Oriented Programming (AOP) для обработки аспектов**

## 🚀 Запуск проекта

Для запуска проекта требуется Java 11 или выше и Maven.

### 1. Клонируйте репозиторий:

```bash
git clone https://github.com/your-repository/SimpleBlogAPI.git
cd SimpleBlogAPI
```

### 2. Соберите проект с помощью Maven:

```bash
mvn clean install
```

### 3. Запустите приложение:

```bash
mvn spring-boot:run
```

После этого приложение будет доступно по адресу: `http://localhost:8080`

## 📑 API

### Статьи

- **Создание статьи**:
    - `POST /articles/create`
    - Описание: Создает новую статью.
    - Параметры запроса: объект статьи (заголовок, контент, дата создания).
    - Ответ: объект созданной статьи.

- **Получение статьи по ID**:
    - `GET /articles/{id}`
    - Описание: Получает статью по ее ID.
    - Параметры: `id` — идентификатор статьи.
    - Ответ: объект статьи.

- **Получение всех статей**:
    - `GET /articles/all`
    - Описание: Получает список всех статей.
    - Ответ: список объектов статей.

- **Получение статей по тегу**:
    - `GET /articles/by-tag`
    - Описание: Получает список статей, которые содержат указанный тег.
    - Параметры: `tagName` — имя тега.
    - Ответ: список статей.

- **Удаление статьи**:
    - `DELETE /articles/{id}`
    - Описание: Удаляет статью по ID.
    - Параметры: `id` — идентификатор статьи.
    - Ответ: отсутствие контента (успех).

- **Добавление тега к статье**:
    - `PUT /articles/{articleId}/tags/{tagId}`
    - Описание: Добавляет тег к статье.
    - Параметры: `articleId` — ID статьи, `tagId` — ID тега.
    - Ответ: обновленная статья.

- **Удаление тега из статьи**:
    - `DELETE /articles/{articleId}/tags/{tagId}`
    - Описание: Удаляет тег из статьи.
    - Параметры: `articleId` — ID статьи, `tagId` — ID тега.
    - Ответ: обновленная статья.

---

### Комментарии

- **Получение комментариев для статьи**:
    - `GET /comments/by-article/{articleId}`
    - Описание: Получает все комментарии для указанной статьи.
    - Параметры: `articleId` — ID статьи.
    - Ответ: список комментариев.

- **Удаление комментария**:
    - `DELETE /comments/{commentId}`
    - Описание: Удаляет комментарий по ID.
    - Параметры: `commentId` — ID комментария.
    - Ответ: отсутствие контента (успех).

- **Очистка кэша комментариев для статьи**:
    - `DELETE /comments/clear-cache/{articleId}`
    - Описание: Очищает кэш комментариев для указанной статьи.
    - Параметры: `articleId` — ID статьи.
    - Ответ: отсутствие контента (успех).

- **Очистка кэша для всех комментариев**:
    - `DELETE /comments/clear-cache-all`
    - Описание: Очищает кэш для всех комментариев.
    - Ответ: отсутствие контента (успех).

- **Создание комментария**:
    - `POST /comments/create`
    - Описание: Создает новый комментарий для статьи.
    - Параметры запроса: объект комментария (контент, связанная статья).
    - Ответ: объект созданного комментария.

---

### Теги

- **Создание тега**:
    - `POST /tags/create`
    - Описание: Создает новый тег.
    - Параметры запроса: объект тега (имя).
    - Ответ: объект созданного тега.

- **Получение тега по ID**:
    - `GET /tags/{id}`
    - Описание: Получает тег по ID.
    - Параметры: `id` — ID тега.
    - Ответ: объект тега.

- **Получение всех тегов**:
    - `GET /tags/all`
    - Описание: Получает список всех тегов.
    - Ответ: список тегов.

- **Поиск тегов по имени**:
    - `GET /tags/search`
    - Описание: Ищет теги по части имени.
    - Параметры: `namePart` — часть имени тега.
    - Ответ: список тегов.

- **Удаление тега**:
    - `DELETE /tags/{id}`
    - Описание: Удаляет тег по ID.
    - Параметры: `id` — ID тега.
    - Ответ: отсутствие контента (успех).

---



## 📂 Структура проекта

```bash
SimpleBlogAPI/
├── .idea/                          # Конфигурации IDE (например, IntelliJ IDEA)
├── Simple-Blog-API/                # Основной каталог проекта
│   ├── .mvn/                       # Каталог для настроек Maven
│   ├── src/                        # Исходный код
│   │   ├── main/                   # Основной исходный код
│   │   │   ├── java/               # Исходные файлы Java
│   │   │   │   ├── com/            # Основной пакет с проектом
│   │   │   │   │   └── example/    # Пакет с вашим приложением
│   │   │   │   │       └── simpleblogapi/  # Корневой пакет проекта
│   │   │   │   │           ├── aspect/           # Аспекты для AOP
│   │   │   │   │           │   └── TagCacheAspect.java # Пример аспектов
│   │   │   │   │           ├── cache/            # Кэширование тегов
│   │   │   │   │           │   └── TagCache.java  # Реализация кэширования
│   │   │   │   │           ├── config/           # Конфигурации Spring
│   │   │   │   │           │   ├── ApplicationConfig.java  # Конфигурация приложения
│   │   │   │   │           │   └── DatabaseConfig.java     # Конфигурация базы данных
│   │   │   │   │           ├── controllers/      # Контроллеры для статей, комментариев, тегов
│   │   │   │   │           │   ├── ArticleController.java     # Контроллер для работы со статьями
│   │   │   │   │           │   ├── CommentController.java     # Контроллер для комментариев
│   │   │   │   │           │   └── TagController.java        # Контроллер для тегов
│   │   │   │   │           ├── entities/         # Сущности для статьи, комментария, тега
│   │   │   │   │           │   ├── ArticleEntity.java   # Сущность для статьи
│   │   │   │   │           │   ├── CommentEntity.java   # Сущность для комментария
│   │   │   │   │           │   └── TagEntity.java       # Сущность для тега
│   │   │   │   │           ├── exceptions/       # Исключения
│   │   │   │   │           │   └── ResourceNotFoundException.java   # Исключение для несуществующих ресурсов
│   │   │   │   │           ├── repositories/     # Репозитории для работы с базой данных
│   │   │   │   │           │   ├── ArticleRepository.java   # Репозиторий для работы со статьями
│   │   │   │   │           │   ├── CommentRepository.java   # Репозиторий для работы с комментариями
│   │   │   │   │           │   └── TagRepository.java      # Репозиторий для работы с тегами
│   │   │   │   │           ├── service/          # Сервисы для бизнес-логики
│   │   │   │   │           │   ├── ArticleService.java      # Сервис для работы со статьями
│   │   │   │   │           │   ├── CommentService.java      # Сервис для работы с комментариями
│   │   │   │   │           │   └── TagService.java          # Сервис для работы с тегами
│   │   │   │   ├── resources/            # Ресурсы (например, application.properties)
│   │   │   │   │   └── application.properties   # Конфигурация Spring приложения
│   │   │   ├── test/                      # Тесты
│   │   │   │   ├── java/                  # Тесты Java
│   │   │   │   │   ├── com/               # Пакет с тестами
│   │   │   │   │   │   └── example/       # Пакет для тестов
│   │   │   │   │   │       └── simpleblogapi/  # Корневой пакет для тестов
│   │   │   │   │   │           ├── ArticleControllerTest.java  # Тесты для контроллеров
│   │   │   │   │   │           ├── TagServiceTest.java         # Тесты для сервисов
│   │   │   │   │   │           └── CommentRepositoryTest.java   # Тесты для репозиториев
│   ├── target/                # Собранные файлы проекта (например, .jar)
├── .gitignore                 # Список игнорируемых файлов для Git
├── pom.xml                    # Файл конфигурации Maven
├── README.md                  # Файл с описанием проекта
└── Simple-Blog-API.iml        # Конфигурация проекта в IntelliJ IDEA
```

## 🚀 Развертывание

Для сборки проекта в `.jar` и его развертывания выполните:

```sh
mvn clean package
java -jar target/SimpleBlogAPI.jar
```

## 📝 Лицензия

Этот проект лицензирован под [MIT License](LICENSE).

































