package com.example.simpleblogapi;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SimpleBlogApiApplicationTests {

	@Test
	void contextLoads() {
		// Этот тест проверяет, что Spring ApplicationContext загружается корректно.
		// Поскольку он не требует выполнения дополнительных проверок, оставляем метод пустым.
		// Если необходимо, добавьте логику для проверки контекста или других аспектов приложения.
		// Если тест не нужен, его можно удалить.
	}
}
